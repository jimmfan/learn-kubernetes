# Coder Templates

This folder is reserved for full Coder workspace templates.

The first useful templates from the walkthrough will be:

- `python-docker`
- `python-kubernetes`

The existing Terraform practice template lives in [../terraform/coder-template-kubernetes](../terraform/coder-template-kubernetes).
