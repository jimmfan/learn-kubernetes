apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: coder-learning
  region: us-east-1

autoModeConfig:
  enabled: true

tags:
  project: learn-kubernetes
  purpose: eks-coder-learning
  owner: james

