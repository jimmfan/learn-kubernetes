# Runner Images

This folder is reserved for custom GitHub Actions runner images.

The first useful image will likely include git, Python, Node, AWS CLI, and any build tools needed by the workflows you want to migrate.
